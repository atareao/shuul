# Test environment (SQLite in-memory)
# Run tests with: cargo test --test integration -- --nocapture
version: "3.8"
services:
  # No external database needed — SQLite lives in-memory or on-disk
  test:
    image: alpine:3.23
    command: echo "SQLite tests need no external service"